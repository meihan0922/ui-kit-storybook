import { Props, Canvas, Story, Preview } from "@storybook/addon-docs";
import { AutoComplete } from "./AutoComplete";

# AutoComplete

<Props components={{ AutoComplete }} />

<h2>搜尋列表，由input與list組合而成，支持非同步promise</h2>
<p>待新增List滾動與keyboard連動</p>
<p>點選項目，關閉選單後，資料更新的時間有待商榷..</p>

<Canvas>
  <Story id="autocomplete--basic" />
  <Story id="autocomplete--render-option" />
  <Story id="autocomplete--promise-fetch" />
</Canvas>
